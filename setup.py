from setuptools import setup

setup(
    name='metagraph',
    version='0.0.1',
    description='Graph algorithm solver across multiple hardware backends',
    author='Anaconda, Inc.',
    packages=['metagraph'],
)
