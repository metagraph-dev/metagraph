name: mg
dependencies:
# dev environment
  - python=3.7
  - coverage
  - pytest
  - pytest-cov
  - black
  - conda-forge::pre-commit

# metagraph dependencies (so setup.py develop doesn't pip install them)
  - importlib_metadata
  - numpy
  - networkx
  - scipy
  - jim22k::grblas
